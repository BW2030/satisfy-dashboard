services:
  - type: web
    name: satisfy-dashboard
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
    autoDeploy: true
    ignoredPaths:
      - data/content.json
